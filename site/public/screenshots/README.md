# Marketing site screenshots

These four files are referenced by the App Preview section on the home page.
Currently they're placeholder copies of the ReWear logo so the page builds
and renders without 404s.

## Replace with real screenshots

Take screenshots from your phone (or the deployed app at app.rewear.ai once
the domain is set), preferably in **portrait, 9:19.5 aspect ratio** so they
match a typical phone frame.

| File | Suggested capture |
|---|---|
| `home.png` | Home with the streak widget visible — tap **Log today's outfit** to make sure the green hero card is in shot |
| `outfit.png` | An outfit result page from `/outfit` — make sure at least one item has a brand visible and the AI reasoning quote shows |
| `friends.png` | `/friends` showing at least one borrow request in the inbox or a "newly connected" friend |
| `schedule.png` | `/schedule` in trip-planner range view with a packing list visible |

PNG, ideally 1080×2340 or similar. JPG is fine too but rename the references
in `src/app/page.tsx` if you do.
