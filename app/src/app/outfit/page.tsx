import { EmptyShell } from "@/components/EmptyShell";

export const metadata = { title: "Outfit — ReWear" };

export default function OutfitPage() {
  return (
    <EmptyShell
      eyebrow="Outfit"
      title="What's the day looking like?"
      description="Pick an occasion. We'll build you an outfit from your wardrobe — prioritising what you've forgotten. AI lands in Session 8."
    />
  );
}
